'use strict';

/**
 * playlist-video-url service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::playlist-video-url.playlist-video-url');
