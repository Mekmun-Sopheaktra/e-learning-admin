'use strict';

/**
 * playlist-video-url router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::playlist-video-url.playlist-video-url');
